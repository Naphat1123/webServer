<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    สาธารณสุขอำเภอบ้านไร่
  </title>
  <link rel="stylesheet" href="../stylesheets/gird.css">
</head>

<body>
  <div class="gird">
    <header class="page-header">

      <%- include ../layout/header.ejs %>

    </header>


    <div class="container bg-light ">
      <div class="row">
        <aside class='col-3'>
          <%- include ../layout/sidebar.ejs %>
        </aside>

        <main class='col-9'>

          <% if(messages.error) { %>
            <div class="alert alert-danger" role="alert">
              <%= messages.error %>
            </div>
            <% } %>
              <% if(messages.success) { %>
                <div class="alert alert-success" role="alert">
                  <%= messages.success %>
                </div>
                <% } %>

                  <div class="mt-3 container">
                    <h3 class="mt-3">
                      ข่าวประชาสัมพันธ์
                    </h3>
                    <br>
                    <a class="btn btn-primary" href="/publicize/add">เพิ่มรายการ</a>
                    <br>

                    <table class=" mt-3 table table-striped table-align-left">

                      <% if(data.length) { %>
                        <thead>
                          <tr>
                            <th scope="col">วันที่</th>
                            <th scope="col">รายการ</th>
                            <th scope="col">ผู้แจ้ง</th>
                          </tr>
                        </thead>

                        <tbody>
                          <% for(let i=0; i < data.length; i++) { %>
                            <tr>
                              <td>
                                <%= data[i].date %>
                              </td>
                              <td>
                                <a href="<%= data[i].file_data %>"><%= data[i].title %></a>
                              </td>
                              <td>
                                <%= data[i].name %>
                              </td>
                            </tr>
                            <% } %>
                        </tbody>
                        <% } %>
                    </table>


                  </div>
        </main>


      </div>

    </div>

    <footer>
      <%- include ../layout/footer.ejs%>
    </footer>

  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-gtEjrD/SeCtmISkJkNUaaKMoLD0//ElJ19smozuHV6z3Iehds+3Ulb9Bn9Plx0x4"
    crossorigin="anonymous"></script>
</body>

</html>